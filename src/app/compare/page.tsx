/**
 * Version Comparison (Diff) Page
 *
 * Route: /compare
 * Layout: Split view 50/50 with toolbar
 *
 * Features:
 * - Version A (left) and Version B (right) selectors
 * - Side-by-side and unified diff toggle
 * - Color-coded additions (green), deletions (red), modifications (yellow)
 * - Synchronized scroll
 * - Summary bar with stats
 * - PDF export
 */
export default function ComparePage() {
  return (
    <div className="container mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold">Compară Versiuni</h1>
      <p className="mt-4 text-muted-foreground">Comparison page - to be implemented</p>
    </div>
  );
}
